# Railway Configuration for Backend Deployment

# Build Configuration
[build]
  builder = "nixpacks"
  buildCommand = "npm install"

# Start Command
[start]
  startCommand = "npm start"

# Health Check
[healthcheck]
  path = "/health"
  interval = 30
  timeout = 10

# Environment
[env]
  NODE_ENV = "production"
